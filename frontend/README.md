# frontend setup
## project creation
npm create vite@latest frontend

## enter project
cd frontend

## install dependencies
npm install
npm install @mui/material @emotion/react @emotion/styled axios react-webcam

## run dev server
npm run dev

## notes

vite gives fast local builds

material ui handles layout

axios sends requests to backend

react-webcam captures images for prediction(but it is not build completely)